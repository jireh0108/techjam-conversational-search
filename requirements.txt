# Core runtime — the committed pipeline (BM25 + popularity prior) needs only these.
PyYAML==6.0.3
numpy==2.4.3          # src/retrieval/ imports numpy unconditionally (dense route, fusion math)

# ML runtime — required when dense retrieval or the cross-encoder ranker is enabled. The ranking
# component still fails soft to NullReranker if these packages are unavailable.
# Install order matters on Windows (torch first avoids a long-path unpack error):
#   pip install torch
#   pip install transformers sentence-transformers huggingface_hub tokenizers
torch==2.13.0
sentence-transformers==6.0.0
# (transformers / huggingface_hub / tokenizers come in as sentence-transformers deps)
# Optional listwise LLM client; only install if ranking.listwise.enabled is explicitly enabled.
# openai
